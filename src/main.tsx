import React from 'react'
import ReactDOM from 'react-dom/client'
import MindMapMVP from './MindMapMVP.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MindMapMVP />
  </React.StrictMode>,
)
